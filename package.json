{
  "name": "chainlinkmeme",
  "private": true,
  "version": "0.0.0",
  "description": "Community-curated archive of Chainlink memes",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter site dev",
    "build": "pnpm --filter site build",
    "worker:dev": "pnpm --filter worker dev",
    "worker:deploy": "pnpm --filter worker deploy",
    "validate": "tsx scripts/validate.ts",
    "manifest": "tsx scripts/build-manifest.ts",
    "og": "tsx scripts/build-og-images.ts",
    "weekly": "tsx scripts/build-weekly.ts",
    "generate-tomls": "tsx scripts/generate-tomls.ts"
  },
  "packageManager": "pnpm@10.18.2",
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "satori": "^0.12.2",
    "smol-toml": "^1.4.2"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
