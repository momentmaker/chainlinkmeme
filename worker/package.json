{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply chainlinkmeme --local",
    "db:migrate:remote": "wrangler d1 migrations apply chainlinkmeme --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250115.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.42.2"
  }
}
